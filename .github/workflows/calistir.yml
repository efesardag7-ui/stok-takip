name: Stok Takip Botu

on:
  schedule:
    # GitHub sunucusuna her 5 dakikada bir çalıştır emri veriyoruz
    - cron: '*/5 * * * *'
  workflow_dispatch: # İstediğimizde elinizle de başlatabilmek için buton ekler

jobs:
  stok-kontrol:
    runs-on: ubuntu-latest
    steps:
      - name: Kodları sunucuya çek
        uses: actions/checkout@v4 # v3'ten v4'e güncellendi (Node 24 Uyumlu)

      - name: Python Kur
        uses: actions/setup-python@v5 # v4'ten v5'e güncellendi (Node 24 Uyumlu)
        with:
          python-version: '3.10'

      - name: Gerekli Kütüphaneleri Yükle
        run: pip install requests beautifulsoup4

      - name: Botu Çalıştır
        run: python bot.py